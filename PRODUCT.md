# Duet · 产品与设计规范

两人私密共享的 Web 应用：同步近况、记录日常、留存共同记忆。  
本文档汇总产品定位、功能需求、页面线框与视觉规范，作为后续设计与开发的基准。

---

## 1. 项目命名

| 项 | 内容 |
|----|------|
| 英文名 | **Duet** |
| 含义 | 二重奏——两人合奏同一段日常 |
| 备选 | Between、Hearth、Loom |

一句话定位：**两人共用的生活日志与同步台。**

---

## 2. 产品定位

Duet 是只属于两个人的私密 Web 空间：同步近况、记日常、留共同记忆。

- 不是聊天软件替代品
- 不是公开社交
- 是「回家就能看见对方今天怎样」的轻量共享桌面

### 2.1 目标用户与场景

| 角色 | 典型时刻 |
|------|----------|
| 你们两人 | 下班后写几句今日；想对方时打开看「对方今日」；周末一起补一条共同回忆 |

核心动机：距离或作息不同时，仍有一个安静、持续的共同语境。

### 2.2 明确不做

- 不做群聊、朋友圈、公开动态
- 不做复杂 IM（语音/视频通话）
- 第一版不做「家庭多人」
- 不做过度游戏化（积分、排行）

---

## 3. 功能需求

### 3.1 已确认功能点（V1 / MVP）

> 确认日期：2026-09-18  
> 原则：配对登录 + 今日同步 + 图文日记 + 时间线 + 基础设置。  
> 「我们」相关能力全部进入 V2，导航中 **不展示**「我们」Tab。

#### A. 账号与空间

| ID | 功能点 | 规则 / 验收要点 | 状态 |
|----|--------|-----------------|------|
| A1 | 注册 | 邮箱 + 密码；密码至少 8 位 | ✅ 确认 |
| A2 | 登录 / 登出 | 登录成功后进入空间流程或「今日」 | ✅ 确认 |
| A3 | 创建空间 | 填写昵称 → 生成邀请码 → 可复制 | ✅ 确认 |
| A4 | 加入空间 | 输入邀请码绑定；一空间仅 2 人 | ✅ 确认 |
| A5 | 配对门禁 | 未配对只能停留在创建/加入页，不能进主应用 | ✅ 确认 |
| A6 | 解除配对 | 设置内危险操作；需二次确认；解绑后双方回到未配对 | ✅ 确认 |
| A7 | 编辑资料 | 昵称、头像（本地上传裁剪为方图即可） | ✅ 确认 |

#### B. 今日同步（产品心脏）

| ID | 功能点 | 规则 / 验收要点 | 状态 |
|----|--------|-----------------|------|
| B1 | 今日双栏 | 桌面左右「我 / 你」；移动端上「你」下「我」 | ✅ 确认 |
| B2 | 更新今日 | Sheet：心情 **3 档表情**（开心/一般/沮丧，**可选**）、一句话（必填，≤ 60 字）、想说的话（可选，≤ 200 字）、配图（可选，最多 1 张） | ✅ 确认 |
| B3 | 每日一条 | 同一用户同一自然日仅一条 CheckIn；再次提交为覆盖更新 | ✅ 确认 |
| B4 | 查看对方 | 只读展示对方今日；无对方内容时安静空态 | ✅ 确认 |
| B5 | 已读状态 | 打开「今日」且对方有内容时，标记己方已读；对方侧可见「未读 / 已读」轻提示 | ✅ 确认 |
| B6 | 空态 | 我未写 / 你未写 / 双方未写，文案与虚线框按线框实现 | ✅ 确认 |
| B7 | 关联日记 | 若当日已有日记，今日页底部一行入口跳转详情或时间线 | ✅ 确认 |

#### C. 日常记录与时间线

| ID | 功能点 | 规则 / 验收要点 | 状态 |
|----|--------|-----------------|------|
| C1 | 写日记 | 日期（默认今天可改）、标题可选、正文、图片 0–9 张 | ✅ 确认 |
| C2 | 日记可见性 | V1 **全部对空间内两人共享**；不做「仅自己」 | ✅ 确认 |
| C3 | 编辑 / 删除 | 仅作者可编辑、删除；删除需确认 | ✅ 确认 |
| C4 | 日记详情 | 全文、大图左右滑、作者与日期 | ✅ 确认 |
| C5 | 时间线 | 按时间倒序合并：日记 Entry + 当日 CheckIn 快照细条 | ✅ 确认 |
| C6 | 筛选 | 全部 / 我的 / 你的 / 按月份 | ✅ 确认 |

#### D. 设置与基础体验

| ID | 功能点 | 规则 / 验收要点 | 状态 |
|----|--------|-----------------|------|
| D1 | 设置页 | 资料、空间信息（含邀请码）、登出、解除配对 | ✅ 确认 |
| D2 | 主题 | V1 **仅浅色（Coastal Dusk）**；深色 / 跟随系统 → V2 | ✅ 确认 |
| D3 | 响应式 | 移动 + 桌面两套导航（底 Tab / 左侧栏）；主栏 max-width 按视觉规范 | ✅ 确认 |
| D4 | 导航范围 | V1 Tab：**今日 \| 记录 \| 我的**（三栏）；无「我们」 | ✅ 确认 |

#### E. V1 明确不做（已确认砍掉或延后）

| 项 | 归属 | 说明 |
|----|------|------|
| 「我们」清单 / 相册；记录内「日子」日历 | V2 | 导航不入口、不占位「即将推出」 |
| 悄悄话留言板 | V2 | — |
| 日记「仅自己可见」 | V2 | — |
| 关键词搜索 | V2 | 时间线筛选足够先用 |
| 深色模式 | V2 | — |
| 推送通知 / 邮件提醒 | V2+ / V3+ | 微信侧采用**小程序订阅消息**（见 §3.3.1）；V1 设置页不放通知开关 |
| 实时 WebSocket | 非必须 | V1 进入页面拉取 + 手动刷新或短轮询即可 |
| 位置签到、共同目标、年度回顾 | 更后 | — |
| 第三方登录（微信等） | 更后 | V1 仅邮箱密码；与小程序登录/绑定一并见 §3.3.1 |
| 多人家庭空间 | 不做 | 产品边界 |

---

### 3.2 V2 功能点（已确认进二期，本阶段不开发）

| ID | 功能点 | 说明 |
|----|--------|------|
| V2-1 | 共同清单 | 分类：想去的 / 想吃的 / 想做的；完成可归档进时间线 |
| V2-2 | 日子（日历标记） | 见 §3.2.1；记录 Tab 下二级入口；原「纪念日」并入本能力 |
| V2-3 | 相册 | 聚合日记图片；时间/来源筛选；灯箱 |
| V2-4 | 悄悄话 | 留言、回复、置顶一句 |
| V2-5 | 日记私密 | 可见性：共享 / 仅自己 |
| V2-6 | 搜索 | 日记正文/标题关键词 |
| V2-7 | 深色主题 | 含跟随系统 |
| V2-8 | 「我们」导航 | 总览入口：清单 / 相册（日子在「记录」下，不进「我们」） |
| V2-9 | 今日刷新 | 见 §3.2.2；短轮询或显式刷新，打开即可看见对方最新今日 |
| V2-10 | 日记草稿 | 见 §3.2.2；写一半可暂存，避免中断丢失 |
| V2-11 | 时间线快捷窗 | 见 §3.2.2；本周 / 本月快捷筛选（补足按月） |
| V2-12 | 日记已读反馈 | 见 §3.2.2；对方打开过你的日记时极轻提示 |

#### 3.2.1 日子（日历标记）· 已确认 2026-09-20

> 两人共享月历上的「日子标记」：出游约定、纪念日、普通标日合一。  
> 不做独立日程 App；不做推送提醒（归 V2+ / 更后）。

| ID | 功能点 | 规则 / 验收要点 | 状态 |
|----|--------|-----------------|------|
| V2-2a | 导航位置 | 主 Tab 仍为 **今日 \| 记录 \| 我的**；「记录」内二级：**时间线 \| 日子** | ✅ 确认 |
| V2-2b | 标记模型 | **不设多类型枚举**；统一为「标记」+ **是否周年**勾选（勾选 = 每年同月同日重复） | ✅ 确认 |
| V2-2c | 字段 | 日期（必填，单日）、标题（必填，≤ 40 字）、备注（可选，≤ 200 字）、周年（布尔）、作者 | ✅ 确认 |
| V2-2d | 单日 | 首版只标**一天**；不做开始–结束跨天行程 | ✅ 确认 |
| V2-2e | 可见与权限 | 空间内两人共享；**双方均可编辑、删除**；删除需确认 | ✅ 确认 |
| V2-2f | 月历 | 月视图；有标记的日期显示色点；点日期看当日标记列表 | ✅ 确认 |
| V2-2g | 写 / 改 | Sheet 或轻页：日期默认点中日或今天；周年默认关（纪念日场景由用户勾选） | ✅ 确认 |
| V2-2h | 列表 | 「即将到来」正序 + 「已过」可折叠；周年按下一周年日计算 | ✅ 确认 |
| V2-2i | 今日轻提示 | 今日页次要区一行：距最近标记 ≤ N 天时展示（如「周六 · 去看海」「还有 4 天 · 在一起」） | ✅ 确认 |

**边界（与清单 / 日记）**

| 能力 | 日子标记 | 日记 Entry | 清单 ListItem |
|------|----------|------------|---------------|
| 主用途 | 标一个具体日期 | 写一段回忆 | 想做但未必有日 |
| 出游 | 「3.28 去看海」标在日历 | 回来后写游记 | 「想去看海」未定日子 |

**首版明确不做**：推送/邮件提醒、跨天区间、第三方日历同步、仅自己可见、与日记强制绑定（可选「写日记」入口即可）。

#### 3.2.2 V2 体验补齐 · 已确认 2026-09-21

| ID | 功能点 | 规则 / 验收要点 | 状态 |
|----|--------|-----------------|------|
| V2-9 | 今日刷新 | 「今日」支持显式刷新；可选短轮询（如 30–60s，可关）；尊重省电与减少打扰 | ✅ 确认 |
| V2-10 | 日记草稿 | 写日记可存草稿（云端或本地，实现期二选一或都支持）；发布后清除草稿；仅作者可见 | ✅ 确认 |
| V2-11 | 时间线快捷窗 | 筛选增加 **本周 / 本月**（与「按月份」并存）；与全部/我的/你的可组合 | ✅ 确认 |
| V2-12 | 日记已读反馈 | 对方打开日记详情后标记已读；作者侧极轻提示（如「已读」）；不做已读回执轰炸 | ✅ 确认 |

**明确不做（2026-09-21）**：语音今日、共同记账、AI 自动写总结（候选 R1–R3 已否决）。

### 3.3 更后期（V3+，仅登记）

| ID | 功能点 | 说明 |
|----|--------|------|
| V3-1 | 微信小程序 + 订阅消息 | **已选通道**；细则见 §3.3.1（2026-09-21）；含纪念日模板 |
| V3-2 | Web Push / 邮件提醒 | 作非微信用户的补充通道（可选） |
| V3-3 | 位置签到 | 隐私可控 |
| V3-4 | 共同目标进度 | — |
| V3-5 | 导出年度回顾 | — |
| V3-6 | 一起回忆 | 随机抽出一篇共享日记，两人可同看；偏仪式感 |
| V3-7 | 心情周报 | 本周双方心情分布一页（仅空间内可见）；克制可视化，无排行 |
| V3-8 | 清单 → 日记草稿 | 勾完清单项后可选「生成日记草稿」；打通 V2-1 与日记 |
| V3-9 | 相册分组 | 日记可选地点/旅行标签；相册按地点或旅行聚合（可与 V3-3 弱关联） |
| V3-10 | 清单完成订阅提醒 | 挂在 V3-1：对方勾完清单项时小程序提醒（需订阅额度） |

> 原「第三方登录（微信等）」并入 V3-1：以小程序为微信入口，Web 邮箱账号可绑定同一 Duet 用户。  
> **已否决**：语音今日、共同记账、AI 自动写总结。

#### 3.3.1 微信小程序订阅消息 · 方向确认 2026-09-21

> **目标**：对方更新「今日」、发来日记、日子/纪念日临近或清单被勾完时，在微信里收到轻提醒，点开回到小程序对应页。  
> **不做**：服务号模板消息作主通道；不做个人号/任意内容群发；V1/V2 不实现。

##### 产品形态

| 项 | 约定 |
|----|------|
| 入口 | Duet **微信小程序**（与现有 Web 共用同一后端与空间数据） |
| 账号 | 小程序 `openid` / `unionid` 绑定既有 User；Web 邮箱账号可在「我的」里绑定微信 |
| 通知通道 | **一次性订阅消息**（用户每次授权可换 1 次发送额度；可引导在关键动作后再次订阅） |
| 开关 | 设置内：接收「对方今日更新」「对方新日记」「日子/纪念日临近」「清单被对方完成」；默认关，需用户主动开并完成订阅 |

##### 可推送事件

| 事件 | 触发 | 消息要点 | 落地页 | 模板 |
|------|------|----------|--------|------|
| 对方更新今日 | 对方保存 CheckIn | 对方昵称、一句话摘要（截断）、时间 | 今日 | 实现期选用/申请 |
| 对方写了日记 | 对方发布 Entry | 对方昵称、标题或首句、日期 | 日记详情 | 实现期选用/申请 |
| 日子 / 纪念日临近 | DayMark 距目标日 ≤ N 天（定时任务） | 见下方已备模板字段映射 | 记录 · 日子 | **已备** 纪念日提醒 |
| 清单被对方完成 | 对方勾完 ListItem（V3-10） | 对方昵称、清单标题、时间 | 我们 · 清单 | 实现期选用/申请 |

模板内**不推送**全文长文、配图 URL；仅摘要 + 跳转。

##### 已备模板：纪念日提醒（2026-09-21）

| 项 | 值 |
|----|-----|
| 标题 | 纪念日提醒 |
| 模版 ID | `Cn46IKptGXFw3Xz1oTbG2QQiCPn2FAQjENuZYwLx_mA` |
| 模版编号 | `35877` |
| 类目 | 信息查询 |

| 模板字段 | 占位符 | 与 DayMark 映射建议 |
|----------|--------|---------------------|
| 纪念日名称 | `{{thing1.DATA}}` | `title`（截断至微信 thing 限制） |
| 纪念类型 | `{{thing6.DATA}}` | 周年 →「周年」；否则「约定」或「标记」（产品文案统一，不做复杂枚举） |
| 日期 | `{{time2.DATA}}` | 目标日 / 下一周年日 |
| 备注 | `{{thing3.DATA}}` | `note`；空则填「点击查看详情」等安全占位 |

场景说明：纪念日提醒。与 V2-2「日子」模型对齐；实现属 **V3**。

##### 规则与边界

1. **先订阅再发送**：无可用额度时不发，可在小程序内轻提示「去开启提醒」。  
2. **仅通知对方**：自己操作不推给自己（日子临近可通知双方，实现期可配置）。  
3. **私密**：订阅文案与跳转不泄露邀请码；解绑微信后停止推送。  
4. **与 Web 关系**：纯 Web 用户无小程序订阅则收不到微信提醒；可后续补邮件 / Web Push（V3-2）。  
5. **明确不做**：朋友圈分享当成通知；服务号群发；未授权强推。

##### 工程要点（实现期再细化，此处定方向）

- 小程序端：`wx.requestSubscribeMessage` → 服务端记录 `openid` + 模板额度/偏好。  
- 服务端：CheckIn / Entry / 清单完成写入成功后异步投递；日子临近由定时任务扫描 DayMark。  
- 实体预留：`WechatBinding`（userId、openid、unionid）、`NotifyPref`（事件开关）、可选 `SubscribeQuota`。

##### 分期

| 阶段 | 范围 |
|------|------|
| V1 / V2 | 不开发、导航与设置不占位「即将推出」 |
| V3 | 小程序壳 + 账号绑定 + 今日/日记/纪念日（已备模板）/ 清单完成订阅 + 设置开关 |

### 3.4 成功标准（已确认）

- 打开后约 3 秒内能看到对方「今天怎样」（含空态）
- 写一条今日同步 ≤ 30 秒
- 周末愿意翻时间线，而不只是打卡
- UI 愿意多待一会儿，工具感不重

### 3.5 概念数据模型（按已确认范围）

**V1 必做实体**

| 实体 | 说明 |
|------|------|
| User | 邮箱、密码哈希、昵称、头像 |
| Space | 两人空间、邀请码、配对状态 |
| SpaceMember | 用户 ↔ 空间（最多 2） |
| CheckIn | 日期、作者、心情、一句话、想说的话、配图、对方已读时间 |
| Entry | 日期、作者、标题、正文、图片列表 |

**V2 再引入**

| 实体 | 说明 |
|------|------|
| ListItem | 清单项、分类、状态、完成时间 |
| DayMark | 日子标记：单日日期、标题、备注、是否周年（`yearly`）、作者；空间内共享，双方可改删 |
| Note | 留言板条目 |
| EntryDraft（可选） | 日记草稿：作者、标题/正文/图片暂存；仅作者可见（V2-10） |
| EntryRead（可选） | 日记已读：entryId、readerId、readAt（V2-12） |

> 原草案实体名 `Milestone` 废弃，统一为 `DayMark`（日历标记）。

### 3.6 信息架构（按已确认导航）

**V1**

```
Duet
├── 今日（默认首页）
├── 记录
│   ├── 时间线
│   ├── 写一条
│   └── 日记详情
└── 我的 / 设置
```

**V2 起增加 / 调整**

```
├── 记录（扩展）
│   ├── 时间线
│   ├── 日子          ← 日历标记（月历 + 列表 + 写/改）
│   ├── 写一条
│   └── 日记详情
├── 我们
│   ├── 清单
│   └── 相册
└── （设置内：主题、通知等）
```

---

## 4. 页面线框说明

### 4.1 全局框架

```
┌─────────────────────────────────────────┐
│  Top bar（品牌 / 空间名 / 头像入口）        │  h ≈ 56–64
├─────────────────────────────────────────┤
│                                         │
│              主内容区                     │
│                                         │
├─────────────────────────────────────────┤
│  Tab：今日 | 记录 | 我的（V1）             │  仅移动端底栏
└─────────────────────────────────────────┘
```

- **桌面端**：左侧窄导航（品牌 + V1 三个入口），右侧主内容；无底栏
- **移动端**：顶栏极简 + 底栏三 Tab（V2 再增「我们」）
- **统一规则**：主内容最大宽约 720–880px 居中；大屏左右留白，不做多栏仪表盘
- 「我们」相关线框见下文，**V1 不实现、不展示入口**

---

### 4.2 登录 / 配对

#### 登录页

```
┌──────────────────────────────────┐
│                                  │
│            D U E T               │  品牌主信号（大）
│         两人的日常二重奏            │  一句副文案
│                                  │
│     [ 邮箱 / 手机 ]               │
│     [ 密码 / 验证码 ]             │
│     [ 进入 ]                      │
│                                  │
│     还没有空间？创建 / 接受邀请      │
│                                  │
└──────────────────────────────────┘
```

- 全屏氛围底（柔和光感或安静生活图），不要功能卡片堆叠
- CTA 一个主按钮即可

#### 创建 / 加入空间

```
┌──────────────┐  ┌──────────────┐
│  创建空间       │  │  加入对方     │
│  取一个昵称   │  │  输入邀请码   │
│  生成邀请码   │  │  确认绑定     │
│  复制给对方   │  │              │
└──────────────┘  └──────────────┘
```

- 绑定成功 → 直达「今日」
- 未配对时「今日」显示空态：等待对方加入

---

### 4.3 今日（默认首页）★ 产品心脏

```
┌─────────────────────────────────────────────────┐
│  Duet                          3月18日 · 周三     │
├──────────────────────┬──────────────────────────┤
│                      │                          │
│   我                  │   你                     │
│                      │                          │
│  ┌────────────────┐  │  ┌────────────────────┐  │
│  │ 心情点 · 精力   │  │  │ 心情点 · 精力       │  │
│  │                │  │  │                    │  │
│  │ 「今天开会到很晚」│  │  │ 「图书馆写到傍晚」  │  │
│  │                │  │  │                    │  │
│  │ 14:20 已同步    │  │  │ 18:02 · 未读→已读  │  │
│  └────────────────┘  │  └────────────────────┘  │
│                      │                          │
│  [ 更新今日 ]         │  （只读，或点进看详情）    │
│                      │                          │
├──────────────────────┴──────────────────────────┤
│  今日想说的话（可选展开区）                         │
│  ┌─────────────────────────────────────────────┐ │
│  │ 多几行的短留言，可附 1 张图                    │ │
│  └─────────────────────────────────────────────┘ │
│                                                   │
│  —— 今日相关 ——                                    │
│  · 若有日记：今日已写 1 条 → 跳转记录               │
│  · 若有日子临近：极轻一行「周六 · 去看海」等（V2）   │
└─────────────────────────────────────────────────┘
```

| 区块 | 内容 | 规则 |
|------|------|------|
| 日期眉题 | 今天日期 | 不可抢过品牌 |
| 双栏「我 / 你」 | 一句话状态 + 可选心情 | 移动端上下叠放：先「你」后「我」 |
| 同步元信息 | 时间、已读 | 已读用极轻点/文字，不做红点轰炸 |
| 主 CTA | 「更新今日」 | 打开编辑 Sheet |
| 次要区 | 今日留言 / 关联日记 / 临近日子（V2） | 不超过 1–2 行 |

**空态**

- 我未写：左栏虚线框 +「写一句今天」
- 对方未写：右栏安静留白 +「TA 今天还没同步」
- 双方都未写：双栏对称空态，不摆假数据

#### 更新今日 Sheet

```
┌─────────────────────────────┐
│  更新今日              [×]  │
│  心情  😄 😐 😞（单选 3 档，可选）   │
│  一句话  [______________]   │  限 40–60 字
│  想说的话 [多行可选]         │  限 200 字
│  [加一张图]（可选）          │
│           [ 同步 ]          │
└─────────────────────────────┘
```

---

### 4.4 记录

#### 时间线（记录 Tab 默认）

```
┌─────────────────────────────────────┐
│  记录     时间线 | 日子（V2）  [ + 写一条 ]│
│  筛选：全部 | 我的 | 你的 | 月份 ▾   │
├─────────────────────────────────────┤
│  3月                                  │
│  ┌─────────────────────────────────┐ │
│  │ 3.18  我                         │ │
│  │ 标题或首句…                      │ │
│  │ [缩略图横滑 0–3]                 │ │
│  └─────────────────────────────────┘ │
│  ┌─────────────────────────────────┐ │
│  │ 3.17  你                         │ │
│  │ …                                │ │
│  └─────────────────────────────────┘ │
│  · 今日同步快照也可作为细条插入：     │
│    「3.17 同步 · 我：… / 你：…」      │
└─────────────────────────────────────┘
```

- 竖向时间流，不是卡片墙网格
- 条目之间用日期/细线分隔；作者用名字或小色点区分
- 「+ 写一条」：顶栏右侧（桌面）或右下轻浮层（移动，勿挡底栏）
- V2：顶栏二级切换 **时间线 | 日子**；日子页的「+」为「标记一天」

#### 写一条 / 编辑

```
┌─────────────────────────────────────┐
│  ← 返回              发布 / 保存     │
│  日期 [今天 ▾]                       │
│  标题（可选）                         │
│  ─────────────────────────────────  │
│  正文（大编辑区，占主视觉）            │
│                                     │
│  ─────────────────────────────────  │
│  图片 [+] [+] [+]                   │
│  可见：共享 ▾（MVP 可锁死「共享」）   │
└─────────────────────────────────────┘
```

#### 日记详情

```
┌─────────────────────────────────────┐
│  ←     3.18 · 我          [···]     │
│  标题                                 │
│  正文                                 │
│  大图（全宽，可左右滑）                │
│  ─────────────────────────────────  │
│  （可选 P1）对方可留一句回应           │
└─────────────────────────────────────┘
```

- `[···]`：编辑、删除（仅作者）

#### 日子（日历标记，V2）

```
┌─────────────────────────────────────┐
│  记录     时间线 | 日子      [ + 标记 ]│
│  ← 3月 2026 →                        │
├─────────────────────────────────────┤
│  日  一  二  三  四  五  六            │
│  ·   ·   ·   ·   ·   ·   1            │
│  2   3●  4   5   6   7   8            │  ● = 有标记
│  …                                   │
├─────────────────────────────────────┤
│  3 日 · 2 条标记                      │
│  · 去看海（一次性）          可改删    │
│  · 在一起（周年）· 下一轮还有 4 天     │
│  ─────────────────────────────────  │
│  即将到来                             │
│  3.22  在一起 · 还有 4 天（周年）      │
│  3.28  去看海                         │
└─────────────────────────────────────┘
```

**写 / 编辑标记 Sheet**

```
┌─────────────────────────────┐
│  标记一天              [×]  │
│  日期  [今天 ▾]             │  仅单日
│  标题  [______________]     │  必填 ≤ 40
│  备注  [多行可选]            │  ≤ 200
│  ☐ 每年重复（周年）          │
│           [ 保存 ]          │
└─────────────────────────────┘
```

- 双方均可编辑、删除；删除二次确认
- 格子色点克制，勿做成厚日历皮肤

---

### 4.5 我们（V2，线框预留，本阶段不开发）

#### 总览

```
┌─────────────────────────────────────┐
│  我们                                 │
│  ┌─────────────┐ ┌─────────────┐     │
│  │ 清单         │ │ 相册         │     │
│  │ 3 件待办     │ │ 128 张       │     │
│  └─────────────┘ └─────────────┘     │
│  （日子在「记录」二级 Tab，不在此入口） │
│  （入口用文字+数字，少用厚卡片）        │
└─────────────────────────────────────┘
```

#### 清单

```
┌─────────────────────────────────────┐
│  清单              [ + ]   分类 ▾    │
│  想去的 / 想吃的 / 想做的             │
│  ○ 去看海一次              我加的    │
│  ○ 试那家拉面              你加的    │
│  ✓ 周末散步（已完成，可归档）         │
└─────────────────────────────────────┘
```

- 完成时可问：是否生成一条时间线条目
- 若清单项已定具体日期，引导去「日子」标记（弱关联，不强制同步）

#### 相册

```
┌─────────────────────────────────────┐
│  相册          按时间 | 按来源        │
│  [图] [图] [图] [图]                 │  紧凑网格，无卡片边框
│  [图] [图] [图] [图]                 │
│  点开 → 灯箱，显示来自哪条日记         │
└─────────────────────────────────────┘
```

---

### 4.6 我的 / 设置

```
┌─────────────────────────────────────┐
│  我的                                 │
│  头像 · 昵称 · 编辑资料               │
│  ─────────────────────────────────  │
│  空间                                 │
│    空间名、配对状态、邀请码             │
│  ─────────────────────────────────  │
│  退出登录                             │
│  （危险区）解除配对                    │
└─────────────────────────────────────┘
```

> V1 不提供主题切换与通知开关（见 §3.1 D2 / E）。

---

### 4.7 关键路径

```
登录 → 配对？
        ├─ 否 → 创建/加入空间 → 今日
        └─ 是 → 今日
今日 → [更新今日] → Sheet → 保存 → 回到今日
今日 → 点「今日已写日记」→ 日记详情
记录 → [+写一条] → 编辑页 → 发布 → 时间线
记录 → 点条目 → 详情 → 编辑/删除
（V2）记录 → 日子 → 月历 / 标记 Sheet → 保存
我的 → 资料 / 空间 / 登出 / 解绑
（V2）我们 → 清单 / 相册
```


---

### 4.8 响应式

| 断点 | 今日双栏 | 导航 | 写日记 |
|------|----------|------|--------|
| &lt; 768 | 上「你」下「我」 | 底 Tab | 全屏编辑 |
| ≥ 768 | 左右并排 | 左侧栏 | 居中栏 + 顶栏操作 |

---

### 4.9 页面对齐已确认功能点

| 阶段 | 页面 |
|------|------|
| V1 必做 | 登录/注册、创建空间、加入空间、今日、更新今日 Sheet、时间线、写日记、日记详情、设置（含解绑） |
| V2 | 记录二级「日子」（月历/标记）、我们总览、清单、相册、悄悄话、深色主题等 |

---

## 5. 视觉规范 v1

### 5.1 设计方向

- **调性**：克制、私密、editorial（私人杂志，不像恋爱社交 App）
- **记忆点**：双栏「我 / 你」用两套近邻色区分身份，全站贯穿
- **品牌优先**：登录页、今日顶栏，`Duet` 字重与字号高于普通标题
- **先看对方**：移动端今日页「你」在上、「我」在下
- **少卡片**：用留白、细线、字阶分区
- **真实影像**：相册/日记图作视觉锚点；装饰渐变不当主角

**刻意避开**：紫靛渐变、奶油底 `#F4F1EA` + 陶土色衬线套餐、报纸密栏、大圆角胶囊、多层阴影、爱心/emoji 装饰、霓虹 glow。

---

### 5.2 色彩 · Coastal Dusk（海岸黄昏）

冷雾灰底 + 深海苔品牌色 + 暖褐作「你」。

#### 浅色（默认主主题）

| Token | Hex | 用途 |
|-------|-----|------|
| `--bg` | `#E6EAED` | 页面底 |
| `--bg-elevated` | `#F5F6F4` | 主内容区、Sheet 底 |
| `--ink` | `#12151A` | 主文字 |
| `--ink-secondary` | `#5C6570` | 次要说明、时间戳 |
| `--ink-tertiary` | `#8B939C` | 占位、禁用 |
| `--line` | `rgba(18,21,26,0.10)` | 分割线、空态描边 |
| `--line-strong` | `rgba(18,21,26,0.16)` | 输入框边框 |
| `--brand` | `#1F4A45` | 品牌、主按钮、关键链接 |
| `--brand-soft` | `#D8E4E1` | 主按钮浅底、选中浅底 |
| `--me` | `#1F4A45` | 「我」身份色（= brand） |
| `--you` | `#6B5344` | 「你」身份色 |
| `--me-soft` | `#DCE8E5` | 我侧浅底 |
| `--you-soft` | `#EDE6E0` | 你侧浅底 |
| `--signal` | `#A6854D` | 「刚更新 / 未读」轻提示 |
| `--danger` | `#8F3A3A` | 删除、解除配对 |
| `--focus` | `#1F4A45` | focus ring |

#### 深色（可选）

| Token | Hex |
|-------|-----|
| `--bg` | `#0E1214` |
| `--bg-elevated` | `#171C1F` |
| `--ink` | `#E8ECF0` |
| `--ink-secondary` | `#9AA3AB` |
| `--brand` | `#8FBFB4` |
| `--me` | `#8FBFB4` |
| `--you` | `#C4A890` |
| `--line` | `rgba(232,236,240,0.10)` |
| `--signal` | `#C4A574` |

#### 使用规则

- 页面允许极轻径向氛围：`bg` 上叠一层低透明度 `--brand`（≤ 4%）
- 「我 / 你」不要大块色背景；用左侧 2px 色条 + 名字色 + soft 底
- 照片上方勿叠半透明营销色块

#### CSS 变量示例

```css
:root {
  --bg: #E6EAED;
  --bg-elevated: #F5F6F4;
  --ink: #12151A;
  --ink-secondary: #5C6570;
  --ink-tertiary: #8B939C;
  --line: rgba(18, 21, 26, 0.10);
  --line-strong: rgba(18, 21, 26, 0.16);
  --brand: #1F4A45;
  --brand-soft: #D8E4E1;
  --me: #1F4A45;
  --you: #6B5344;
  --me-soft: #DCE8E5;
  --you-soft: #EDE6E0;
  --signal: #A6854D;
  --danger: #8F3A3A;
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 16px;
  --space: 4px;
  --shadow-sheet: 0 12px 40px rgba(18, 21, 26, 0.12);
}
```

---

### 5.3 字体

| 角色 | 字体 | 用途 |
|------|------|------|
| Display | **Instrument Serif** | 品牌词 `Duet`、日期大字、空态短句 |
| UI / Body | **Manrope** | 导航、正文、按钮、表单 |
| Mono | **IBM Plex Mono** | 邀请码 |

Fallback：Display → `Georgia`；UI → `system-ui`。

#### 字阶

| Token | 规格 | 场景 |
|-------|------|------|
| `display-brand` | Serif / 40–48 / 1.1 / tracking 0.02em | 登录页 Duet |
| `display-date` | Serif / 28–32 / 1.2 | 今日页日期 |
| `title-lg` | Manrope 600 / 22–24 / 1.3 | 页面标题 |
| `title-md` | Manrope 600 / 17–18 / 1.35 | Sheet 标题、日记标题 |
| `body` | Manrope 400 / 15–16 / 1.65 | 日记正文 |
| `body-sm` | Manrope 400 / 13–14 / 1.5 | 时间戳、辅助 |
| `label` | Manrope 500 / 12 / 1.4 / +0.04em | 「我」「你」、Tab、label |
| `button` | Manrope 600 / 14–15 / 1 | 按钮文字 |

- 主栏 `max-width: 720px`（今日、设置）；时间线可到 `800px`
- 正文行宽保持舒适，避免通栏拉满

---

### 5.4 间距与版式

Spacing scale（×4）：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`

| 场景 | 值 |
|------|-----|
| 组件内边距 | 12–16 |
| 区块间距 | 24–32 |
| 页面左右边距（移动） | 20 |
| 顶栏高度 | 56（移动）/ 64（桌面） |
| 底 Tab 高度 | 56 + safe-area |

- 今日双栏：桌面 `1fr 1fr`，中间 `1px` `--line`；间隙 `24–32`
- 桌面导航：左侧 `200–220px` 固定

---

### 5.5 圆角、线、阴影

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-sm` | 6px | 输入框、小控件 |
| `--radius-md` | 10px | Sheet、图片缩略 |
| `--radius-lg` | 16px | Sheet 顶角等 |
| 头像 | 建议 8px 微方，全站统一 | |

- 线：默认 `1px solid var(--line)`
- 阴影只用一档 `--shadow-sheet`；按钮、列表项、日记条无阴影
- 禁止多层投影、禁止 glow

---

### 5.6 组件要点

| 组件 | 规范 |
|------|------|
| 顶栏 | 左 Serif `Duet`；右日期或头像；底 1px `--line` |
| Tab / 侧栏 | 默认 `--ink-secondary`；选中 `--brand`；可用 2px 短线指示，不用胶囊 |
| 主按钮 | `bg --brand`，字浅色，高 44，圆角 sm |
| 今日栏 | 左侧 2px me/you 色条；空态 dashed `--line` |
| Sheet | 遮罩 `rgba(18,21,26,0.28)`；底 `--bg-elevated` |
| 时间线条目 | 无卡片阴影；上下分割线；作者色 label |
| 表单 | 高 44；focus 边框 brand + soft 外环 |
| 相册网格 | `gap: 4–6`，无圆角白边相框 |

图标：细线 1.5–2px（如 Lucide），色随文字；选中随 `--brand`。

---

### 5.7 动效（仅 3 处）

| 名称 | 规格 | 场景 |
|------|------|------|
| `enter-fade` | opacity 0→1，`translateY(6px→0)`，220ms，`ease-out` | 进入今日、时间线首屏 |
| `sheet-up` | 遮罩 fade 180ms；面板 `translateY(100%→0)` 280ms | 更新今日 |
| `signal-soft` | `--signal` 小点呼吸 2.4s，仅「未读」 | 对方新同步 |

遵循 `prefers-reduced-motion: reduce` 时取消位移与脉冲。

---

### 5.8 页面套用速查

| 页面 | 背景 | 主视觉 | 强调色 |
|------|------|--------|--------|
| 登录 | `--bg` + 轻氛围图 | Serif 品牌 | 单主按钮 `--brand` |
| 今日 | `--bg`，内容区 elevated | 双栏 + 日期 Serif | me/you 左边线 |
| 记录 | 同今日 | 「+ 写一条」主按钮；V2 二级「日子」月历 | 作者色 label；标记色点克制 |
| 写日记 | elevated 全屏感 | 正文 Manrope | 顶栏「发布」 |
| 我们 | 同今日 | 文字入口 + 数字（清单/相册） | 少色块 |
| 设置 | 同今日 | 分组列表 | danger 仅解绑 |

---

### 5.9 可访问性

- 正文与底对比 ≥ 4.5:1
- 心情点除颜色外加 `aria-label`
- 焦点可见
- 尊重减少动效偏好

---

### 5.10 工程落地建议

1. `tokens.css`（或 Tailwind theme extend）承载上表变量  
2. 引入 Instrument Serif + Manrope（+ IBM Plex Mono 用于邀请码）  
3. 优先实现组件：`Button` / `TabBar` / `TodayColumn` / `EntryRow` / `Sheet`  

---

## 6. 文档状态

| 项 | 状态 |
|----|------|
| 产品定位 | 已定稿 |
| **功能点确认（V1 / V2 / V3）** | **§3.1–3.3（2026-09-18）；日子 §3.2.1（09-20）；V2 体验补齐 §3.2.2 与 V3-6～10、纪念日模板 §3.3.1（09-21）** |
| 页面线框 | 已定稿（导航已按 V1 三 Tab 对齐；V2 日子挂记录二级） |
| 视觉规范 v1 | 已认可 |
| 技术栈 / 工程骨架 | 见 TECH_STACK.md |

下一步建议：V1 已收齐并上线；下一迭代优先 V2（日子 + §3.2.2）；V3 小程序与订阅消息（含已备纪念日模板）。
